name: Polybot Build Dev

on:
  push:
    branches:
     - dev3
  workflow_dispatch:
  pull_request:
    branches:
      - dev3

jobs:
  Build:
    runs-on: ubuntu-latest
    environment: production
    outputs:
      output1: ${{ steps.step1.outputs.image }}
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          echo "Building Docker image for dev3 branch"

      - id: step1
        run: |
          docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" -p "${{ secrets.DOCKERHUB_TOKEN }}"
          img_name="/polybot_build_dev:${{ github.run_number }}"
          echo "image=$img_name" >> "$GITHUB_OUTPUT"
          echo "Building Docker image: ${{ secrets.DOCKERHUB_USERNAME }}$img_name"
          docker build --push -t "${{ secrets.DOCKERHUB_USERNAME }}$img_name" .

  DockerScoutScan:
    if: github.event_name == 'pull_request' && github.base_ref == 'main'
    runs-on: ubuntu-latest
    needs: Build
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker image for scanning
        run: |
          docker build -t polybot-temp-image .

      - name: Docker Scout Scan
        id: scout
        uses: docker/scout-action@v1
        with:
          command: cves
          image: polybot-temp-image
          only-severities: critical,high
          summary: true
          github-token: ${{ secrets.GITHUB_TOKEN }}
          exit-code: 1

      - name: Add PR comment with scan summary
        if: always()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: docker-scout-report
          message: |
            🕵️ **Docker Scout Report**
            Scan completed for the Docker image.

            *Critical/High issues will fail this PR.*
            ${{ steps.scout.outputs.summary }}

  Deploy:
    needs: Build
    uses: ./.github/workflows/deploy_polybot.yaml
    with:
      image: ${{ needs.Build.outputs.output1 }}
    secrets:
      EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
      EC2_HOST: ${{ secrets.EC2_HOST_DEV }}
      EC2_USERNAME: ${{ secrets.EC2_USERNAME }}
      APP_CERT: ${{ secrets.APP_DEV_CERT }}
      TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN_DEV }}
      S3_BUCKET: ${{ secrets.S3_BUCKET_DEV }}
      DOCKER_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      APP_NGINX_URL: ${{ secrets.APP_DEV_NGINX_URL }}
      QUEUE_URL: ${{ secrets.QUEUE_URL_DEV }}